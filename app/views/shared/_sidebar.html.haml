.sidebar
  - if current_user
    %ul
      %li 
        = link_to "Discussions", root_url, :class => ( current_page?( root_url ) ? "active" : "")
      %li
        = link_to "Inbox", root_url
      %li 
        = link_to "Directory", users_url, :class => ( current_page?( users_url ) ? "active" : "")
      %li 
        = link_to "Calendar", root_url
    %ul
      %li 
        = link_to "Suggestion Box", root_url
      %li 
        = link_to "The Vault", root_url
    %ul
      %li 
        = link_to "Profile", root_url
      %li
        = link_to "Sign Out", logout_url, 'data-no-turbolink' => true
  - else
    %ul
      %li 
        = link_to "Sign Up", new_user_url
      %li 
        = link_to "Log In", login_url
